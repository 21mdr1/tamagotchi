import './Egg.scss';

export default function Egg() {
    return (
        <div 
            title="an egg, it is waiting to hatch" 
            className="egg"
            // style={{'--steps': '2'}}
        />
    );
}